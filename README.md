
  # Color Wheel

  This is a code bundle for Color Wheel. The original project is available at https://www.figma.com/design/5zcfeoEHdup6bnieUKpxME/Color-Wheel.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  